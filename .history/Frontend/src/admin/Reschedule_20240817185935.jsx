import {} from 'react'
import { useParams } from 'react-router-dom'

const Reschedule = () => {
    const {id} = useParams();
    const getEvents 
  return (
    <div>
      
    </div>
  )
}

export default Reschedule
