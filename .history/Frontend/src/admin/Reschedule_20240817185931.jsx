import {} from 'react'
import { useParams } from 'react-router-dom'

const Reschedule = () => {
    const {id} = useParams();
    const get
  return (
    <div>
      
    </div>
  )
}

export default Reschedule
